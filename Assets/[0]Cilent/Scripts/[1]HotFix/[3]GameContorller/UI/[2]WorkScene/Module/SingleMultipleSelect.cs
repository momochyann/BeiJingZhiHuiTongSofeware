using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SingleMultipleSelect : SelectBaseUI
{
    // Start is called before the first frame update
    void Start()
    {
        Init().Forget();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
